server:
  port: 7003

eureka:
  instance:
    hostname: eureka7003.com #集群环境下要求名称不一样，此处注意windows环境下hosts文件的配置
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka